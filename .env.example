# Backend server URL (for production, set this to your Railway/Render backend)
# If not set, defaults to window.location.origin (works if backend is co-located)
# VITE_BACKEND_URL=https://your-backend.railway.app

# Server port (for standalone backend)
PORT=3001
